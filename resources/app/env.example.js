/**
 *  Variables d'environnement
 */
(function (window) {

  window.__env = window.__env || {};

  // URL de la webapp
  window.__env.webappUrl = '/';

  // URL de l'API
  window.__env.apiUrl = '/api/v1/';

}(this));
